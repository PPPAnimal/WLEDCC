update manual

wled native phone app updates instantly to changes,
some other way to check status of wled devices
stream status live from wled device?
or just more frequent pings?

drag drop of card should allow screen to scroll up/down if dragged to edge of border

do mh report mode-7 color fade on ping?
could show on effect picker


**********************************************************
DONE, TESTING
**********************************************************
check box to top of log screen beside debug off
if checked debug mode on by default when log opens
add check box beside close log
log auto opens at program start on checked box

master slider currently can show in row1 wide mode and row2 narrow mode
i want to remove it from narrow mode and have it always show in row1
remove master slider for row2, only show it in row1

i made a fix recently for loading scenes
they used to take forever to load
the new system for scenes dispatches commands to all devices
then loops back to ping them while and resend if needed
and reconcile before reporting success or list of devices that failed
to update
i need this solution applied to the all on and all off buttons

change color on MH results in brightness changing to 100%.
should send a brightness change after the color request that matches current 
slider position?

on start ledfx, toggle to ledfx scene list for view
can remove the auto load of scenes and just use the
function that is on button activation which loads scene list also.
line 506 - delayed scene fetch
fetch ledfx scenes

minimum brightness for app name 
should be raised higher, still to dark at lowest setting.
set constraint, min 25% brightness on cards and app name in top bar, device sliders should be unaffected by this change.

scenes fail to set a device running preset effect to solid color request
need to set effect solid, then color

does scan need 30 secs?
most devices are picked up instantly, 
those missed come up next scan instantly.
maybe do a couple 10 sec fast scans
not one slow 30sec one?

need to detect if a copy of wledcc.exe is running
and refuse to start second.
maybe bring running exe to front/forground 
so its visable

live badge should be purple, wled scenes toggle, same blue as wled badge
5026 is orange code

on startup, if no ledfx path in json, look in program files, likely paths
if found, save path to json and update button to not ask for 
install or show me path.

ledfx_monitor_loop not detecting offline devices.
unified loop should still be pinging all wled devices
so why is it not detecting offline.
i clicked adjusted brightness on offline device and still
not detected as offline
got log, ceiling failed all 3 attempts
still shows online

when adding custom cards
we have area to enter ip, path to exe, or url
i want to add a way to browse to the EXE
click a button and popup lets you find the exe
then enters that path into card

limit logs and json files, 
atm its writing a new file after each change

moved wled update button beside slider
moved live badge button beside slider
increased minimum brightness on borders and app name to 25%

wait for ledfx to fully load before
toggle scenes switch that starts scene download.
set to 5 secs from 3

JSON - missing, loads backup
corrupted, fails with no error and starts fresh.
fixed

upgrading from older versions of this app results
in losing saved json settings
mainly the saved scenes
is there a way to notice the failed json load
and if no backup exists, pull out what data we can like
device names, scenes etc and then create a new updated
json merging old data with new

update buttons should show version to download
make reboot and sanitize look like buttons








******************************
MINOR CHANGES PLANNED
******************************
line 390 
def _ledfx_virtual_to_ip(self, vid, virtuals):
        """Legacy fallback — no longer used by main poll loop."""
        """Legacy fallback — no longer used by main poll loop."""
        return None
may be able to delete

update manual with new features
explain in user terms, not programmer
better formatting

add auto backup preset and config
add auto upload   "           "

note on log screen, after close log, 
small text, 
clearing log only clears screen data, 
logs are still saved in file

on exiting app
display popup
add all off button
runs same code as main all off button
add stop ledfx button
runs same code as main stop ledfx button
add cancel button
closes popup, leaves app open
add exit button
closes app


try master brightness slider on same row as app name, centered
may be fixed with new measure process

manual
some text alignment is off in manual
title for sections in caps, color green
	features name under title in caps color cyan
		indents on new lines that are part of above feature

***************************
MAJOR CHANGES PLANNED
***************************
your text here, 
vu meter here, 

Scene thumbnails or color swatches showing what a scene looks like before activating it

Grouping — tag devices into rooms and control a whole room at once

Schedules — turn things on/off or activate scenes at set times

Mobile build once ready to tackle that
